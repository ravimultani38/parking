{
  "scripts": {
    "format": "prettier . --check",
    "lint": "eslint .",
    "start": "node app.js",
    "dev": "node --watch --env-file=.env app.js",
    "build": "cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongodb": "^6.11.0",
    "mongoose": "^8.8.1",
    "nodemon": "^3.1.7"
  }
}
